import styled from "styled-components";

const StyledBookCollection = styled.section`
		display: flex;
		flex-wrap: wrap;
		justify-content: space-evenly;

		width: 90%;
		margin: auto;

		text-align: center;
`

export default StyledBookCollection